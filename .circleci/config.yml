version: 2
jobs:
  test:
    docker:
      - image: ruby:2.6.3-alpine
    steps:
      - checkout
      - run: echo 'run test'
      - run: bundle install
      - run: bundle exec ruby test/natto2classifier_test.rb
workflows:
  version: 2
  test:
    jobs:
      - test
